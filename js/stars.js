(function(){'use strict';const canvas=document.getElementById('star-field');if(!canvas)return;const ctx=canvas.getContext('2d');let width,height,stars=[],galaxies=[],dpr;function resize(){dpr=Math.min(window.devicePixelRatio||1,2);width=window.innerWidth;height=window.innerHeight;canvas.width=width*dpr;canvas.height=height*dpr;canvas.style.width=width+'px';canvas.style.height=height+'px';ctx.setTransform(dpr,0,0,dpr,0,0);}function createStars(){const count=Math.floor((width*height)/4000);stars=[];for(let i=0;i<count;i++){stars.push({x:Math.random()*width,y:Math.random()*height,r:Math.random()*1.2+0.3,alpha:Math.random()*0.6+0.2,twinkleSpeed:Math.random()*0.02+0.005,twinklePhase:Math.random()*Math.PI*2,hue:Math.random()>0.85?(Math.random()>0.5?160:30):0,sat:Math.random()>0.85?40:0,});}}function createGalaxies(){const count=Math.floor(width/500)+1;galaxies=[];for(let i=0;i<count;i++){galaxies.push({x:Math.random()*width,y:Math.random()*height,size:Math.random()*20+10,alpha:Math.random()*0.04+0.01,rotation:Math.random()*Math.PI,});}}let time=0;function draw(){ctx.clearRect(0,0,width,height);time+=0.016;galaxies.forEach(g=>{ctx.save();ctx.translate(g.x,g.y);ctx.rotate(g.rotation);const gr=ctx.createRadialGradient(0,0,0,0,0,g.size);gr.addColorStop(0,`rgba(0,168,120,${g.alpha*1.5})`);gr.addColorStop(0.5,`rgba(0,168,120,${g.alpha*0.5})`);gr.addColorStop(1,'rgba(0,168,120,0)');ctx.fillStyle=gr;ctx.scale(1,0.4);ctx.beginPath();ctx.arc(0,0,g.size,0,Math.PI*2);ctx.fill();ctx.restore();});stars.forEach(s=>{const tw=Math.sin(time*s.twinkleSpeed*60+s.twinklePhase)*0.3+0.7;const a=s.alpha*tw;ctx.fillStyle=s.hue>0?`hsla(${s.hue},${s.sat}%,80%,${a})`:`rgba(220,230,240,${a})`;ctx.beginPath();ctx.arc(s.x,s.y,s.r,0,Math.PI*2);ctx.fill();});requestAnimationFrame(draw);}function init(){resize();createStars();createGalaxies();draw();}window.addEventListener('resize',()=>{resize();createStars();createGalaxies();});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();})();
